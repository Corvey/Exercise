#include <stdio.h>
#include <math.h>
int main(void)
{
    int n, x, sum;
    scanf("%d", &n);
    while(n--)
    {
        scanf("%d", &x);
        if(x==10)
            printf("7\n");
        else
        {
            x %= 1000;
            for(int i=1; i<=9; ++i)
            {

            }
        }
    }
}
